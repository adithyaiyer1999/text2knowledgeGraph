# Creates the react Website

Currently, uses a preexisting HTML template to output 

## To be added :

1. Access Django API via POST requests to run the backend

## To run the repo

```
 npm start
```
